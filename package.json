{
  "name": "elysia-boilerplate",
  "version": "0.1.3",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun run --watch src/main.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --outfile build/server src/main.ts",
    "migration:generate": "bun run --bun drizzle-kit generate",
    "migration:apply": "bun run --bun drizzle-kit migrate",
    "migration:studio": "bun run --bun drizzle-kit studio"
  },
  "main": "src/main.ts",
  "dependencies": {
    "@bogeychan/elysia-logger": "^0.1.10",
    "@elysiajs/openapi": "^1.4.2",
    "drizzle-orm": "^0.44.5",
    "drizzle-typebox": "^0.3.3",
    "elysia": "^1.4.4",
    "envalid": "^8.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "drizzle-kit": "^0.31.4",
    "pino-pretty": "^13.1.1"
  }
}
